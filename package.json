{
  "name": "minimemory-do-demo",
  "version": "0.1.0",
  "private": true,
  "description": "minimemory WASM in Cloudflare Durable Objects — benchmark vs D1",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "bench": "node benchmark/bench.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.100.0"
  }
}
